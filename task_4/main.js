/**
 * Created by Виталий on 13.10.2016.
 */

$(function() {
    var sum = [];

    $('.stat__body tr').each(function(i) {
        var $this = $(this);

        $this.find('.js-count').each(function(j) {

        });
    });
});